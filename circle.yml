machine:
  timezone:
    Australia/Perth

  php:
    version: 5.4.5

dependencies:
  post:
    # Update our dependencies
    - composer install

test:
  override:
    # Run PHPCS
    - php bin/phpcs -n --standard=./vendor/magento-ecg/coding-standard/Ecg Location